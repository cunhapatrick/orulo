import styled from 'styled-components';
import { Layout } from 'antd';

export const Footer = styled(Layout.Footer)`
	background: black;
	color: white;
	height: 75px;
	font-weight: bold;
	font-size: 16;
	clear: both;
`;
